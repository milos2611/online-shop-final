export class ProductModel  {
    
    constructor(

        public id:number,  public name: string, public description: string, public numberOfProduct: number, public image: string, public price: number) {
    }
}

